# Demeter Mobile

Application Android Compose reconstruite pour le flux mobile Demeter Sante:
login, accueil, enregistrement/import audio, transcription optionnelle avec assignation, generation des trois comptes rendus et envoi email.

La configuration backend est lue depuis `.env.production` via `BACKEND_BASE_URL`.
